# Nest js

## Dependency Injection (DI)

`Dependency Injection হলো একটি Design Pattern যেখানে একটি ক্লাস তার প্রয়োজনীয় অবজেক্ট বা ডিপেন্ডেন্সি নিজে তৈরি না করে বাইরের কোনো সোর্স থেকে গ্রহণ করে।`

- AuthController-এর যদি AuthService প্রয়োজন হয়, তবে সে নিজে new AuthService() লিখে অবজেক্ট বানাবে না। বরং সে শুধু বলবে "আমার এটা লাগবে", আর NestJS তাকে সেটা সরবরাহ করবে।


## কেন আমরা এটা করি? (The Problem it Solves)

`DI ব্যবহার না করলে দুটি প্রধান সমস্যা হয়:`

1. Tight Coupling: যদি আমি ক্লাসের ভেতরেই new AuthService() লিখি, তবে আমি ওই ক্লাসের সাথে শক্তভাবে আটকে গেলাম। ভবিষ্যতে যদি আমি সার্ভিসটা পরিবর্তন করতে চাই, তবে আমাকে প্রতিটি ফাইলে গিয়ে কোড চেঞ্জ করতে হবে।
2. Hard to Test: ইউনিট টেস্টিং করার সময় আমি আসল ডাটাবেস সার্ভিস ব্যবহার না করে একটি "Fake/Mock" সার্ভিস ব্যবহার করতে চাই। DI ছাড়া এটা করা অসম্ভব।

## NestJS-এ এটি কীভাবে কাজ করে? (The Mechanism)

`NestJS মূলত ৩টি ধাপে DI সম্পন্ন করে:`

1. Declaration: আমরা সার্ভিসের ওপর @Injectable() লিখি। এর মানে হলো, "এই ক্লাসটি ইনজেক্ট হওয়ার যোগ্য"।
2. Registration: আমরা মডিউলের (Module) providers অ্যারেতে সার্ভিসটিকে লিস্ট করি। এর ফলে NestJS-এর IoC (Inversion of Control) Container এই ক্লাসের একটা রেকর্ড বা ব্লুপ্রিন্ট নিজের কাছে রেখে দেয়।
3. Injection: যখন কন্ট্রোলারের constructor-এ আমরা টাইপ ডিফাইন করি (যেমন: private authService: AuthService), NestJS রানটাইমে দেখে যে এই কন্ট্রোলার চালাতে AuthService দরকার। সে তখন নিজের স্টোররুম (IoC Container) থেকে ওই সার্ভিসের একটি Singleton Instance (একদম তাজা একটি কপি) বের করে কন্ট্রোলারের হাতে ধরিয়ে দেয়।


## Coupling

`Coupling মানে হলো "নির্ভরশীলতা" (Dependency)`

1. Tight Coupling (শক্ত বাঁধন)
যখন একটি ক্লাস বা মডিউল অন্য একটি ক্লাসের ওপর মারাত্মকভাবে নির্ভরশীল হয়, তখন তাকে Tight Coupling বলে।

- বাস্তব উদাহরণ: আপনার ফোনের ব্যাটারি যদি ফোনের বডির সাথে আঠা দিয়ে লাগানো থাকে (যেমন আধুনিক স্মার্টফোন), তবে সেটা Tight Coupling। এখন ব্যাটারি নষ্ট হলে আপনাকে পুরো ফোনটাই খুলতে হবে বা অনেক ক্ষেত্রে ফোনটাই ফেলে দিতে হবে। আপনি চাইলেই অন্য কোনো কোম্পানির ব্যাটারি ওখানে লাগাতে পারবেন না।

- কোডে সমস্যা: আপনি যদি ক্লাসের ভেতরেই new AuthService() লিখে অবজেক্ট তৈরি করেন, তবে ওই ক্লাসটি AuthService-এর সাথে টাইটলি কাপলড। পরে যদি আপনি AuthService পরিবর্তন করে AdvancedAuthService আনতে চান, তবে আপনাকে কোডের ভেতর হাত দিতেই হবে।

```ts
// ১. একটি নির্দিষ্ট ইমেইল সার্ভিস
class NodemailerService {
  send(message: string) {
    console.log(`Sending Email using Nodemailer: ${message}`);
  }
}

// ২. রেজিস্ট্রেশন ক্লাস
export class UserRegistration {
  private emailService: NodemailerService;

  constructor() {
    // ❌ ভুল: এখানেই সরাসরি অবজেক্ট তৈরি করা হচ্ছে (Tight Coupling)
    this.emailService = new NodemailerService();
  }

  register() {
    this.emailService.send('Welcome to our platform!');
  }
}
```
- সমস্যা কী? ভবিষ্যতে যদি আপনার কোম্পানি বলে, "আমরা আর Nodemailer ব্যবহার করবো না, এখন থেকে SendGrid ব্যবহার করবো"—তাহলে আপনাকে UserRegistration ক্লাসের ভেতরে গিয়ে কোড পরিবর্তন করতে হবে। এটা অনেক রিস্কি।

2. Decoupling / Loose Coupling

যখন একটি মডিউল অন্যটির ওপর সরাসরি নির্ভর না করে একটি "মাধ্যম" বা ইন্টারফেসের মাধ্যমে কাজ করে, তখন তাকে Decoupling বা Loose Coupling বলে।

- বাস্তব উদাহরণ: আপনার ল্যাপটপের USB Port। এখানে আপনি মাউস লাগাতে পারেন, কিবোর্ড লাগাতে পারেন, আবার পেনড্রাইভও লাগাতে পারেন। মাউস নষ্ট হলে আপনার ল্যাপটপ ফেলে দিতে হয় না, শুধু মাউসটা খুলে অন্য একটা মাউস লাগিয়ে দিলেই হয়। ল্যাপটপ জানে না আপনি কী লাগাচ্ছেন, সে শুধু জানে এটা একটা USB ডিভাইস।
- কোডে সুবিধা: NestJS-এ আমরা যখন constructor-এ সার্ভিস ইনজেক্ট করি, তখন আমরা সরাসরি অবজেক্ট তৈরি করি না। আমরা শুধু বলি "আমার এই টাইপের একটা কিছু লাগবে"। এর ফলে কোড অনেক বেশি ফ্লেক্সিবল হয়।

___
- Flexibility: সহজেই একটা পার্ট সরিয়ে অন্যটা লাগানো যায়।
- Maintenance: একটা মডিউলে কোড চেঞ্জ করলে অন্য মডিউল ভেঙে যাওয়ার ভয় থাকে না।
- Testing: টেস্ট করার সময় আসল সার্ভিসের বদলে ডামি (Mock) সার্ভিস ব্যবহার করা যায় (যেটা আমরা DI নিয়ে আলাপ করার সময় বলেছি)।
___

## Object-Oriented Programming (OOP)

1. Constructor

- একটি ক্লাস যখন তৈরি হয়, তখন সবার আগে যে ফাংশনটি নিজে নিজে কল হয়, সেটিই হলো constructor। এটি মূলত ক্লাসের ভেরিয়েবলগুলোকে "প্রাথমিক মান" (Initial value) দেওয়ার জন্য ব্যবহৃত হয়।

বাস্তব উদাহরণ: আপনি যখন একটি ফোন কিনছেন, ফোনটি ফ্যাক্টরি থেকে বের হওয়ার সময় তার কিছু ডিফল্ট সেটিংস (যেমন: ব্রাইটনেস ৫৯%, ল্যাঙ্গুয়েজ ইংলিশ) সেট করা থাকে। এই কাজটি ফ্যাক্টরির constructor করে দিয়েছে।

2. Private ও Public (এক্সেস কন্ট্রোল)

- Public: এটি ডিফল্ট। যে কেউ বাইরে থেকে এই ডাটা বা ফাংশন দেখতে পারবে।
- Private: এটি শুধুমাত্র ওই ক্লাসের ভেতরেই ব্যবহার করা যাবে। বাইরের কেউ এটা দেখতে বা পরিবর্তন করতে পারবে না।

```ts
class BankAccount {
  public userName: string = "Abir";
  private balance: number = 5000; // এটা বাইরে থেকে দেখা যাবে না
}

const myAcc = new BankAccount();
console.log(myAcc.userName); // ✅ কাজ করবে
console.log(myAcc.balance);  // ❌ এরর দেবে (কারণ এটি Private)
```

3. This

this মানে হলো "এই ক্লাসের নিজস্ব"। যখন একটি ক্লাসের ভেতরে অনেকগুলো ভেরিয়েবল থাকে, তখন ওই ক্লাসের কোনো ফাংশনের ভেতর থেকে নিজের ভেরিয়েবলকে ডাকতে হলে this ব্যবহার করতে হয়।

```ts
class Robot {
  name = "Chitti";

  sayName() {
    console.log("My name is " + this.name); // 'this' মানে এই রোবটের নাম
  }
}
```

4. Super

যখন একটি ক্লাস অন্য একটি ক্লাসকে Inherit (উত্তরাধিকার সূত্রে পাওয়া) করে, তখন উপরের (Parent) ক্লাসের constructor-কে কল করার জন্য super() ব্যবহার করা হয়।

বাস্তব উদাহরণ: একজন বাবা এবং তার ছেলে। ছেলের নিজের কিছু গুণ আছে, আবার বাবার থেকেও কিছু পেয়েছে। ছেলে যখন তার বাবার গুণগুলো এক্টিভেট করতে চায়, সে super() কল করে।

```ts
class Animal {
  constructor(name: string) {
    console.log(name + " is born");
  }
}

class Dog extends Animal {
  constructor(name: string) {
    super(name); // বাবার (Animal) কনস্ট্রাক্টরকে কল করা হলো
    console.log("Dog is barking");
  }
}
```

5. NestJS-এ এগুলোর ম্যাজিক (Shorthand Constructor)
NestJS-এ আমরা প্রায়ই দেখি: constructor(private readonly authService: AuthService) {}

এখানে আসলে তিনটি কাজ একসাথে হচ্ছে:

- private দিয়ে ভেরিয়েবলটি ডিক্লেয়ার করা হচ্ছে।

- AuthService টাইপ সেট করা হচ্ছে।

- NestJS-এর DI সিস্টেম নিজে থেকেই এই ভেরিয়েবলে ডাটা ইনজেক্ট করে দিচ্ছে। আপনাকে আলাদা করে this.authService = authService লিখতে হচ্ছে না।

___

## OOP (Object-Oriented Programming)

1. Abstraction (অপ্রয়োজনীয় তথ্য লুকিয়ে রাখা)

সহজ মানে: আপনি যখন গাড়ি চালান, আপনি শুধু স্টিয়ারিং, ব্রেক আর এক্সিলারেটর দেখেন। ইঞ্জিনের ভেতরে পিস্টন কীভাবে নড়ছে বা ফুয়েল কীভাবে বার্ন হচ্ছে, সেটা আপনার জানার দরকার নেই।

- কোডে কাজ: জটিল লজিক ভেতরে রাখা এবং বাইরে শুধু সহজ কিছু ফাংশন (যেমন start(), stop()) দেখানো।

- কেন দরকার? যাতে ব্যবহারকারী কনফিউজড না হয় এবং কোড ব্যবহার করা সহজ হয়।

2. Encapsulation (ডাটা প্রোটেকশন বা ক্যাপসুল)

সহজ মানে: একটি ঔষধের ক্যাপসুলের ভেতরে যেমন গুড়া ঔষধগুলো নিরাপদ থাকে, তেমনি ক্লাসের ডাটাগুলোকে Private করে রাখা যাতে বাইরের কেউ ভুলভাল পরিবর্তন করতে না পারে।

- বাস্তব উদাহরণ: আপনার ফোনের batteryPercentage আপনি সরাসরি আঙুল দিয়ে টেনে ১০০% করতে পারেন না। এটা শুধু সিস্টেমের ভেতর থেকে চার্জ হওয়ার পর বাড়ে।

- কোডে কাজ: ভ্যারিয়েবলকে private করা এবং সেগুলো পরিবর্তনের জন্য নির্দিষ্ট public ফাংশন রাখা।

3. Inheritance (উত্তরাধিকার)

সহজ মানে: বাবার কিছু বৈশিষ্ট্য অটোমেটিক সন্তানের মধ্যে চলে আসা। এতে করে একই জিনিস বারবার তৈরি করতে হয় না।

- বাস্তব উদাহরণ: সব "গাড়ি" (Car) এর চাকা আছে এবং হর্ন আছে। এখন আপনি যদি "টয়োটা" বা "বিএমডব্লিউ" বানান, তবে আপনাকে নতুন করে চাকা বা হর্ন বানাতে হবে না। আপনি শুধু Car ক্লাস থেকে এগুলো উত্তরাধিকার সূত্রে নিয়ে নেবেন।

- কোডে কাজ: extends কিউওয়ার্ড ব্যবহার করা।

4. Polymorphism (বহুরূপী)
সহজ মানে: একই জিনিসের আলাদা আলাদা রূপ। 'Poly' মানে অনেক, আর 'Morph' মানে রূপ।

- বাস্তব উদাহরণ: আপনার কাছে একটি Animal ক্লাস আছে যার একটি ফাংশন হলো makeSound(). এখন যদি প্রাণীটি 'বিড়াল' হয় তবে সে বলবে "মিউ", আর যদি 'কুকুর' হয় তবে সে বলবে "ঘেউ"। ফাংশনের নাম একই (makeSound), কিন্তু কাজ আলাদা।

- কোডে কাজ: একই নামের ফাংশন আলাদা আলাদা ক্লাসে আলাদাভাবে লেখা।

```ts
// ১. Inheritance (Parent Class)
class Vehicle {
  constructor(public brand: string) {}

  // ৩. Abstraction: ইউজার শুধু জানবে গাড়ি স্টার্ট হয়, ভেতরে কী হয় জানার দরকার নেই
  public start() {
    this.injectFuel(); // ভেতরের কাজ
    console.log(`${this.brand} is starting...`);
  }

  private injectFuel() {
    console.log("Fuel injected safely.");
  }
}

// ২. Encapsulation: Private ডাটা প্রোটেকশন
class Car extends Vehicle {
  private speed: number = 0; // কেউ বাইরে থেকে সরাসরি speed পাল্টাতে পারবে না

  constructor(brand: string) {
    super(brand); // ৪. Super: বাবার কন্সট্রাক্টর কল করা
  }

  public accelerate() {
    this.speed += 20; // ৫. This: নিজের ক্লাসের ভ্যারিয়েবল
    console.log(`Current speed: ${this.speed} km/h`);
  }
}

// ৪. Polymorphism (একই নামের আলাদা কাজ হতে পারে অন্য ক্লাসে)
const myCar = new Car("Tesla");
myCar.start();      // বাবার থেকে পাওয়া গুণ
myCar.accelerate(); // নিজের গুণ
```

## super() কি কনস্ট্রাক্টরের বাইরে কল করা যায়?

- না, যায় না। যদি আপনি কনস্ট্রাক্টরের ভেতরে প্যারেন্ট ক্লাসের প্রপার্টি বা মেথড এক্সেস করতে চান, তবে super() অবশ্যই কনস্ট্রাক্টরের ভেতরেই থাকতে হবে।

**কেন? (The Reason): JavaScript/TypeScript-এ যখন আপনি একটি ক্লাসকে অন্য একটি ক্লাসের চাইল্ড বানান (extends ব্যবহার করে), তখন চাইল্ড ক্লাসের নিজস্ব কোনো this (নিজস্ব অস্তিত্ব) থাকে না যতক্ষণ না সে তার বাবাকে (Parent Class) জন্ম দিচ্ছে। সহজ কথায়: বাবা না জন্মালে ছেলে আসবে কোত্থেকে? super() কল করার মানে হলো আপনি মেমোরিতে আগে প্যারেন্ট ক্লাসের অবজেক্টটি তৈরি করছেন, তারপর চাইল্ড ক্লাসের this তৈরি হচ্ছে।**

```ts
class Vehicle {
  constructor(public brand: string) {
    console.log("১. আগে বাবা (Vehicle) তৈরি হলো");
  }
}

class Car extends Vehicle {
  constructor(brand: string, public model: string) {
    // console.log(this.model); // ❌ ভুল! super() এর আগে 'this' ব্যবহার করা যাবে না।

    super(brand); // ২. বাবাকে কল করা হলো। এখন 'this' তৈরি হয়েছে।

    console.log("৩. এবার ছেলে (Car) তৈরি হলো");
    this.model = model; // ✅ এখন কাজ করবে।
  }
}

const myCar = new Car("Toyota", "Corolla");
```

- যদি চাইল্ড ক্লাসে কনস্ট্রাক্টর না থাকে? তখন আপনাকে super() লিখতে হবে না। TypeScript অটোমেটিক প্যারেন্ট ক্লাসের কনস্ট্রাক্টরকে কল করে দেবে।
- Order of execution (সিরিয়াল): সবসময় মনে রাখবেন: super() সবসময় কনস্ট্রাক্টরের প্রথম লাইনে হওয়া উচিত (যদি আপনি this ব্যবহার করতে চান)। কারণ বাবাকে আগে জায়গা দিতে হবে, তারপর ছেলের কাজ শুরু হবে।

## constructor

- যদি আপনি চাইল্ড ক্লাসে কোনো constructor না লেখেন, তবে আপনাকে super() লিখতে হবে না। TypeScript বা JavaScript অটোমেটিক আপনার হয়ে পর্দার আড়ালে প্যারেন্ট ক্লাসের কনস্ট্রাক্টরকে কল করে দেবে।

সহজভাবে বললে, "বাবার সম্পত্তি যদি ছেলে আলাদা করে নিজের নামে দাবি না করে (নতুন কনস্ট্রাক্টর না লেখে), তবে সে অটোমেটিক সব পেয়ে যায়।"


1. যখন কনস্ট্রাক্টর নেই (Automatic Access)

`এখানে Car ক্লাসে কোনো কনস্ট্রাক্টর নেই, কিন্তু সে ঠিকই brand এক্সেস করতে পারছে।`

```ts
class Vehicle {
  constructor(public brand: string) {}

  showBrand() {
    console.log(`This is a ${this.brand}`);
  }
}

// চাইল্ড ক্লাসে কোনো কনস্ট্রাক্টর লিখলাম না
class Car extends Vehicle {}

const myCar = new Car("Tesla"); // সে অটোমেটিক বাবার কনস্ট্রাক্টর ব্যবহার করছে
myCar.showBrand(); // আউটপুট: This is a Tesla
```

2. প্যারেন্টের জিনিস অ্যাক্সেস করার উপায়

আপনি যখন extends করেন, তখন আপনি তিনটি উপায়ে বাবার জিনিস অ্যাক্সেস করতে পারেন:

**`this ব্যবহার করে (সবচেয়ে কমন)`**
বাবার প্রপার্টি বা মেথড এখন ছেলেরও। তাই this.brand বা this.showBrand() লিখলেই কাজ হবে।

```ts
class Car extends Vehicle {
  startEngine() {
    console.log(this.brand + " engine started!"); // বাবার brand ব্যবহার করছি
  }
}
```
**সরাসরি কল করে (Method Overriding-এর সময়)**

- যদি চাইল্ড ক্লাসেও একই নামের ফাংশন থাকে (যেমন showBrand), কিন্তু আপনি চাচ্ছেন বাবার অরিজিনাল ফাংশনটাও চলুক, তখন super.methodName() ব্যবহার করবেন।

```ts
class Car extends Vehicle {
  showBrand() {
    super.showBrand(); // বাবার ফাংশন কল হলো
    console.log("This is specifically a Car model."); // নিজের কিছু অ্যাড করলাম
  }
}
```
## সিনারিও ১: চাইল্ড ক্লাসে কোনো কনস্ট্রাক্টর নেই (No Constructor)

`এখানে লক্ষ্য করুন, Car ক্লাসে আমরা কোনো নতুন ডাটা নিচ্ছি না, শুধু বাবার ফাংশনটি ব্যবহার করছি। তাই আমাদের super() বা constructor কিছুই লিখতে হবে না।`

```ts
// প্যারেন্ট ক্লাস
class Vehicle {
  constructor(public brand: string) {}

  showBrand() {
    console.log(`এই গাড়িটি হলো: ${this.brand}`);
  }
}

// চাইল্ড ক্লাস - এখানে আমরা নতুন কিছু যোগ করছি না
class Car extends Vehicle {
  // অটোমেটিক বাবার কন্সট্রাক্টর এবং প্রপার্টি এখানে চলে আসবে
  honk() {
    console.log(`${this.brand} হর্ন দিচ্ছে: পিপ পিপ!`);
  }
}

const myCar = new Car("Toyota");
myCar.showBrand(); // আউটপুট: এই গাড়িটি হলো: Toyota
myCar.honk();      // আউটপুট: Toyota হর্ন দিচ্ছে: পিপ পিপ!
```

## সিনারিও ২: চাইল্ড ক্লাসে নতুন প্রপার্টি আছে (With Constructor & Super)

`এখানে আমরা চাইল্ড ক্লাসে model নামক একটি নতুন জিনিস যোগ করতে চাই। যেহেতু আমরা নতুন ডাটা স্টোর করতে চাই, তাই আমাদের নিজস্ব constructor লাগবে। আর নিয়ম অনুযায়ী, নিজস্ব কন্সট্রাক্টর থাকলে super() দিয়ে বাবাকে খুশি করতে হবে।`

```ts
class Vehicle {
  constructor(public brand: string) {}
}

class Car extends Vehicle {
  // ১. নতুন প্রপার্টি 'model' যোগ করছি
  public model: string;

  constructor(brand: string, model: string) {
    // ২. super(brand) কল না করলে এরর আসবে।
    // কারণ বাবাকে আগে brand পাঠিয়ে তার অস্তিত্ব তৈরি করতে হবে।
    super(brand);

    // ৩. এখন আমরা আমাদের নিজস্ব প্রপার্টি সেট করতে পারবো
    this.model = model;
    console.log("চাইল্ড কন্সট্রাক্টর সাকসেসফুল!");
  }

  showDetails() {
    // brand আসছে বাবা থেকে, model আসছে নিজের থেকে
    console.log(`গাড়ি: ${this.brand}, মডেল: ${this.model}`);
  }
}

const myNewCar = new Car("Tesla", "Model 3");
myNewCar.showDetails(); // আউটপুট: গাড়ি: Tesla, মডেল: Model 3
```

### 🏗️ Class Inheritance Scenarios | ক্লাস ইনহেরিটেন্স সিনারিও

অবজেক্ট ওরিয়েন্টেড প্রোগ্রামিংয়ে (OOP) চাইল্ড ক্লাসে কন্সট্রাক্টর ব্যবহার করা এবং না করার মধ্যে যে পার্থক্য রয়েছে, তা নিচে তুলে ধরা হলো:

| বিষয় (Topic) | সিনারিও ১: No Constructor | সিনারিও ২: New Constructor |
| :--- | :--- | :--- |
| **কখন ব্যবহার করবেন?** (When to use?) | **English:** When no extra data is needed beyond the parent's properties.<br>**বাংলা:** যখন বাবার প্রপার্টি ছাড়া নতুন কোনো ডাটা লাগবে না। | **English:** When child class needs new data (e.g., model, color).<br>**বাংলা:** যখন চাইল্ড ক্লাসে একদম নতুন কোনো ডাটা যোগ করতে হবে। |
| **Super() কি লাগবে?** (Need super()?) | **English:** No, it's not required.<br>**বাংলা:** না, দরকার নেই। | **English:** Yes, must be called on the very first line.<br>**বাংলা:** অবশ্যই লাগবে এবং প্রথম লাইনেই লাগবে। |
| **নতুন মেথড/ফাংশন** (New Methods) | **English:** Can be added easily.<br>**বাংলা:** অনায়াসে যোগ করা যায়। | **English:** Can be added easily.<br>**বাংলা:** অনায়াসে যোগ করা যায়। |

---

## DI

`constructor(private readonly appService: AppService) {}`

1. Parameter Property (TypeScript ম্যাজিক)
সাধারণত একটি ক্লাসে ভেরিয়েবল ব্যবহার করতে হলে আমাদের ৩টি কাজ করতে হয়:

- উপরে ভেরিয়েবল ডিক্লেয়ার করা: private appService;

- কনস্ট্রাক্টরে প্যারামিটার নেওয়া: constructor(appService: AppService)

- ভ্যালু অ্যাসাইন করা: this.appService = appService;

**কিন্তু আপনি যখন কনস্ট্রাক্টরের ভেতরেই private বা public লিখে দেন, তখন TypeScript এই ৩টি কাজ এক লাইনেই করে ফেলে। এটাকে বলে Shorthand Syntax।**

2. Dependency Injection (NestJS ম্যাজিক)
এখানে আপনি কিন্তু বলেননি this.appService = new AppService(). আপনি শুধু লিখেছেন appService: AppService (টাইপ হিসেবে)।

- কাজ: NestJS যখন এই কন্ট্রোলার বা ক্লাসটি রান করে, সে দেখে যে আপনার AppService প্রয়োজন। সে তখন অটোমেটিক পর্দার আড়ালে AppService-এর একটি অবজেক্ট তৈরি করে আপনার কনস্ট্রাক্টরে পাঠিয়ে দেয়। আপনাকে কষ্ট করে new কিউওয়ার্ড দিয়ে অবজেক্ট বানাতে হয় না।

3. Access Modifiers (private)
- private লেখার মানে হলো, এই appService শুধুমাত্র এই ক্লাসের ভেতরেই (this.appService) ব্যবহার করা যাবে। ক্লাসের বাইরে থেকে কেউ একে এক্সেস করতে পারবে না। এটি Encapsulation নিশ্চিত করে।

4. readonly (ইমিউটেবিলিটি)
readonly মানে হলো, একবার এই appService-এ ভ্যালু ঢুকে গেলে আপনি ভুল করেও ক্লাসের ভেতরে অন্য কোথাও একে পরিবর্তন করতে পারবেন না (যেমন: this.appService = null লিখতে পারবেন না)। এটি কোডকে অনেক বেশি সেফ রাখে।

**IoC Container (Inversion of Control)।**


## DTO (Data Transfer Object)

`DTO (Data Transfer Object) হলো আপনার অ্যাপ্লিকেশনের "বর্ডার গার্ড" বা "চেকপোস্ট"।`

`@Body() registerUserDto: RegisterUserDto`

- এখানে @Body() হলো ডেকোরেটর যা রিকোয়েস্টের ডাটা ধরে আনে, আর RegisterUserDto হলো সেই ডাটার "শেপ" বা "ডিজাইন"।

1. DTO আসলে কী? (What is DTO?)
- DTO বা Data Transfer Object হলো একটি সাধারণ ক্লাস যা নির্ধারণ করে একটি নেটওয়ার্ক রিকোয়েস্টে (যেমন POST বা PUT) কী কী ডাটা আসবে এবং তাদের টাইপ কী হবে।

- সহজ উদাহরণ: ইউজার যখন রেজিস্ট্রেশন করে, সে হয়তো name, email, আর password পাঠায়। এই তিনটা জিনিসের একটা সেট বা অবজেক্টই হলো DTO।

2. কেন লাগে? না থাকলে সমস্যা কী? (The "Why")
যদি আপনি DTO ব্যবহার না করেন, তবে আপনি আসলে আপনার ড্রয়িংরুমের দরজা সবার জন্য খোলা রাখলেন।

- Security Risk: ইউজার যদি ডাটার সাথে role: 'ADMIN' পাঠিয়ে দেয় এবং আপনি সরাসরি সেই ডাটা ডাটাবেসে সেভ করেন, তবে সে হ্যাকার না হয়েও এডমিন হয়ে যেতে পারে! DTO থাকলে আপনি শুধু নির্দিষ্ট ফিল্ডগুলোই গ্রহণ করবেন।

- Validation: ইউজার ইমেইলের জায়গায় শুধু একটা নাম পাঠিয়ে দিল, অথবা পাসওয়ার্ড দিল মাত্র ১ অক্ষরের। DTO ছাড়া আপনাকে প্রতিবার ফাংশনের ভেতরে অনেক if-else লিখে এগুলো চেক করতে হতো।

- Type Safety: আপনার কোড জানবে না registerUserDto.email আসলে কি স্ট্রিং নাকি নাম্বার। DTO থাকলে টাইপস্ক্রিপ্ট আপনাকে অটো-সাজেশন দেবে এবং ভুল করলে এরর দেখাবে।

3. DTO দেখতে কেমন হয়? (Example)

`NestJS-এ আমরা class-validator লাইব্রেরি ব্যবহার করে DTO-কে শক্তিশালী করি:`

```ts
import { IsEmail, IsString, MinLength } from 'class-validator';

export class RegisterUserDto {
  @IsString()
  name: string;

  @IsEmail({}, { message: 'সঠিক ইমেইল দিন' })
  email: string;

  @IsString()
  @MinLength(8, { message: 'পাসওয়ার্ড অন্তত ৮ অক্ষরের হতে হবে' })
  password: string;
}
```

## "Interface কেন ব্যবহার করি না DTO-র জন্য?"

- উত্তর: ইন্টারফেস শুধু কম্পাইল টাইমে থাকে, রানটাইমে (যখন অ্যাপ চলে) ভ্যানিশ হয়ে যায়। কিন্তু DTO হলো Class, যা রানটাইমেও থাকে। ফলে class-validator দিয়ে আমরা রানটাইমে ডাটা ভ্যালিডেট করতে পারি।

## "DTO ব্যবহার করলে কি সিকিউরিটি বাড়ে?"

- উত্তর: হ্যাঁ। DTO-র সাথে NestJS-এর ValidationPipe এবং whitelist: true অপশন ব্যবহার করলে, ইউজার যদি এক্সট্রা কোনো আজেবাজে ডাটা পাঠায়, NestJS সেগুলো অটোমেটিক ফিল্টার করে ফেলে দেয়। একে Mass Assignment Vulnerability থেকে সুরক্ষা বলা হয়।

## Data Transfer Object আর Entity-র মধ্যে পার্থক্য কী?

- উত্তর: DTO হলো ইউজারের থেকে আসা ডাটার ফরম্যাট। আর Entity হলো ডাটাবেসের টেবিলের ফরম্যাট। সব সময় ইউজারের পাঠানো সব ডাটা ডাটাবেসে যায় না (যেমন: confirmPassword শুধু চেক করার জন্য আসে, সেভ করার জন্য না)।

## npm install class-validator class-transformer
- class-validator: এর কাজ হলো চেক করা (যেমন: @IsEmail(), @MinLength())। ডাটা ঠিক না থাকলে সে এরর মেসেজ দেয়। ২. class-transformer: এর কাজ হলো রূপান্তর করা। ধরুন, ইউজার আপনার এপিআই-তে স্ট্রিং হিসেবে id পাঠালো, কিন্তু আপনার ডিটিও-তে টাইপ দেওয়া আছে number। এই প্যাকেজটি সেই স্ট্রিংকে অটোমেটিক নাম্বারে রূপান্তর (Transform) করে দেয়।
