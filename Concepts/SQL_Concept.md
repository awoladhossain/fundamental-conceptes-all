## 🎯 SQL IN Operator

**কাজ:** একটি কলামের ভ্যালু একাধিক নির্দিষ্ট ভ্যালুর সাথে ম্যাচ করাতে ব্যবহার হয়।
- IN
```bash
SELECT * FROM Customers
WHERE city IN ('Dhaka', 'Chittagong', 'Sylhet');
```
- NOT IN

```bash
SELECT * FROM Customers
WHERE city NOT IN ('Dhaka', 'Sylhet');
```

**সুবিধা:**
- কোড অনেক ছোট এবং পরিষ্কার হয়।
- অনেকগুলো `OR` ব্যবহারের ঝামেলা কমে।
- সাব-কুয়েরির সাথে খুব ভালো কাজ করে।

**সিনট্যাক্স:**
`SELECT column_name FROM table_name WHERE column_name IN (value1, value2, ...);`


## 🔍 Filter Operators: BETWEEN & LIKE



| অপারেটর | কাজ | উদাহরণ |
| :--- | :--- | :--- |
| **BETWEEN** | নির্দিষ্ট রেঞ্জ বা সীমানা ফিল্টার করে। | `price BETWEEN 10 AND 50` |
| **LIKE** | প্যাটার্ন বা আংশিক লেখা খুঁজে বের করে। | `name LIKE 'A%'` |

**টপ টিপস:** - `LIKE` দিয়ে সার্চ করলে কেস-সেন্সিটিভিটি (Case-sensitivity) খেয়াল রাখবেন (Postgres-এ বড় হাত-ছোট হাতের জন্য `ILIKE` ব্যবহার করা হয়)।
- `NOT BETWEEN` বা `NOT LIKE` ব্যবহার করে এর উল্টো ফিল্টারও করা যায়।

### 💡 SQL Common Mistakes & Fixes

1. **BETWEEN vs IN**:
   - নাম্বার বা তারিখের রেঞ্জ হলে `BETWEEN` ব্যবহার করুন।
   - নির্দিষ্ট কয়েকটা নাম বা টেক্সট হলে `IN` ব্যবহার করুন।

2. **COUNT(column) vs COUNT(*)**:
   - `COUNT(city)` শুধু ওই সিটির ভ্যালুগুলো গুনবে (NULL থাকলে বাদ দিবে)।
   - `COUNT(*)` পুরো রো গুনবে, যা সাধারণত বেশি নিরাপদ।

3. **Multi-Table Joins**:
   - যখনই এক টেবিলের ডেটা দিয়ে অন্য টেবিলের হিসাব করতে হয়, তখনই `JOIN` করতে হয়।
   - নামগুলো ছোট করার জন্য Alias (যেমন `Customers c`) ব্যবহার করা ভালো।
