# From Yaml to Running POD

1. `USER COMMAND (awolad@noob)`: সবার শুরুতে আপনার ল্যাপটপে বসা এই আপনি! আপনি kubectl apply -f my-pod.yaml কমান্ড দিলেন।


2. `API SERVER`: আপনার কমান্ডটি সরাসরি গেল CONTROL PLANE-এর গেটওয়েতে। এপিআই সার্ভার রিকোয়েস্টটি নিয়ে নিল।


3. `PERSISTENCE (etcd)`: এপিআই সার্ভার সাথে সাথে আপনার অর্ডারের তথ্য ডাটাবেসে (etcd database) সেভ করে রাখলো। এই তথ্যটা ক্লাস্টারের "Source of Truth"।

4. `SCHEDULING (kube-scheduler)`: শিডিউলার দেখলো একটা নতুন পড চেয়েছে। সে তখন ক্লাস্টারের অবস্থা চেক করলো এবং ঠিক করলো আপনার পডটি docker-desktop নোডে যাবে।


5. `EXECUTION (Kubelet)`: এপিআই সার্ভার এবার আপনার নোডের ভেতরের এজেন্ট Kubelet-কে খবর দিল। কিউবেলেট দেখলো তার নোডে কাজ এসেছে।

6. `CONTAINER CREATION`: কিউবেলেট তখন কন্টেইনার রানটাইমকে (containerd/Docker) হুকুম দিল কন্টেইনার তৈরি করতে। কন্টেইনার তৈরি হয়ে গেল।

7. `STATE REPORTING`: সবশেষে, কিউবেলেট এপিআই সার্ভারকে খবর পাঠালো— "বস, কাজ হয়ে গেছে, পড এখন রানিং!" এপিআই সার্ভার ডাটাবেস আপডেট করলো, আর আপনি টার্মিনালে দেখলেন READY 1/1, Running।

![alt text](image.png)